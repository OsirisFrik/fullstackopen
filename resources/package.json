{
  "name": "resources",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "persons": "json-server --watch persons.json --port 3001",
    "notes": "json-server --watch notes.json --port 3002"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.0"
  }
}
